*.fasl
*~
settings.lisp
!docker/settings.lisp
# the built CLI binary at the repo root (scripts/ichiran-cli is a source file,
# so this rule is anchored to the root on purpose)
/ichiran-cli
/ichiran-cli.exe
docker/pgdata/
# local perf env
settings.lisp
!docker/settings.lisp

# local perf env artifacts
local-env/
